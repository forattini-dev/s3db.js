import Database from "../database.class.js";

export class Plugin {
  setup(s3db) {
    // Implementação do método setup (se necessário)
  }

  start() {
    // Implementação do método start (se necessário)
  }
}

export default Plugin;
