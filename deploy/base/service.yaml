apiVersion: v1
kind: Service
metadata:
  name: s3db-mcp
  labels:
    app: s3db-mcp
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: s3db-mcp
